#include <unistd.h>

/**
 * mul - multiplication of two integers.
 * @a: integer
 * @b: integer
 * Return: On success 1
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	return (a * b);
}
