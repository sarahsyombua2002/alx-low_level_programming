#include "main.h"

/**
 * _islower - lowercase characters
 * Returns: 1 for lowercase character or 0 for anyhing else
 */

int _islower(int c)

{
if (c >= 97 && c <= 122)returb;
{
return (1);
}
return (0);
}
