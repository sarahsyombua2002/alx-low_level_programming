#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - computation of absolute value of an integer
 * @n: input number as an integer
 * Return: absolute value
 */

int _abs(int n)
{
	return (abs(n));
}
